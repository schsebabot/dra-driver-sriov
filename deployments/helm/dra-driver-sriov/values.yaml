# Default values for dra-driver-sriov.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""
namespaceOverride: ""
selectorLabelsOverride: {}

allowDefaultNamespace: false

imagePullSecrets: []
image:
  repository: ghcr.io/k8snetworkplumbingwg/dra-driver-sriov
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

controller:
  priorityClassName: "system-node-critical"
  podAnnotations: {}
  podSecurityContext: {}
  nodeSelector:
    node-role.kubernetes.io/control-plane: ""
  tolerations:
  - key: node-role.kubernetes.io/master
    operator: Exists
    effect: NoSchedule
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
    effect: NoSchedule
  affinity: {}
  containers:
    controller:
      securityContext: {}
      resources: {}

kubeletPlugin:
  priorityClassName: "system-node-critical"
  updateStrategy:
    type: RollingUpdate
  podAnnotations: {}
  podSecurityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  kubeletRegistrarDirectoryPath: /var/lib/kubelet/plugins_registry
  kubeletPluginsDirectoryPath: /var/lib/kubelet/plugins
  nriPluginName: dra-driver-sriov
  nriPluginIndex: 42
  defaultInterfacePrefix: vfnet
  configurationMode: STANDALONE
  enableDeviceMetadata: false
  # Host path for CNI plugin binaries. Could also be /var/lib/cni/bin.
  cniBinHostPath: /opt/cni/bin
  sriovCniInit:
    # Install upstream sriov-cni via init container. Disable on OpenShift when the
    # SR-IOV Network Operator already provides the sriov-cni binary.
    enabled: true
    image: ghcr.io/k8snetworkplumbingwg/sriov-cni:v2.10.0
  metrics:
    # Enable the controller-runtime Prometheus metrics endpoint.
    enabled: false
    # The host-network port used when metrics are enabled.
    port: 8080
  containers:
    init:
      securityContext: {}
      resources: {}
    plugin:
      securityContext:
        privileged: true
      resources: {}
      # Port running a gRPC health service checked by a livenessProbe.
      # Set to a negative value to disable the service and the probe.
      healthcheckPort: -1

# Logging configuration
logging:
  # Log verbosity level (0-9, higher means more verbose)
  level: 3
  # Log format: text or json
  format: text
  # Whether to log to stderr (in addition to any log files)
  alsologtostderr: true
  # Optional: log file path (if empty, logs only to stderr)
  logFile: ""

# webhook:
#   enabled: false
#   servicePort: 443
#   containerPort: 443
#   priorityClassName: "system-cluster-critical"
#   strategy:
#     type: RollingUpdate
#   podAnnotations: {}
#   podSecurityContext: {}
#   nodeSelector: {}
#   tolerations: []
#   affinity: {}
#   containers:
#     webhook:
#       securityContext:
#         privileged: false
#       resources: {}
#   serviceAccount:
#     # Specifies whether a service account should be created
#     create: true
#     # Annotations to add to the service account
#     annotations: {}
#     # The name of the service account to use.
#     # If not set and create is true, a name is generated using the fullname template
#     name: ""
